<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sales_trip.list">
      <field name="name">sales_trip list</field>
      <field name="model">sales_trip.sales_trip</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sales_trip.action_window">
      <field name="name">sales_trip window</field>
      <field name="res_model">sales_trip.sales_trip</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sales_trip.action_server">
      <field name="name">sales_trip server</field>
      <field name="model_id" ref="model_sales_trip_sales_trip"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sales_trip" id="sales_trip.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sales_trip.menu_1" parent="sales_trip.menu_root"/>
    <menuitem name="Menu 2" id="sales_trip.menu_2" parent="sales_trip.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sales_trip.menu_1_list" parent="sales_trip.menu_1"
              action="sales_trip.action_window"/>
    <menuitem name="Server to list" id="sales_trip" parent="sales_trip.menu_2"
              action="sales_trip.action_server"/>
    -->
  </data>
</odoo>